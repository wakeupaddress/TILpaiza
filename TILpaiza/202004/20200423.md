D039:正三角形かどうか

```
<?php
    // 自分の得意な言語で
    // Let's チャレンジ！！

    $one = fgets(STDIN);
    $two = fgets(STDIN);
    $three = fgets(STDIN);
    
    if($one==$two&&$one==$three&&$two==$three){
        echo "YES";
    }
    else{
        echo "NO";
    }
?>
```