D047:メダリストの表示

```
<?php
    // 自分の得意な言語で
    // Let's チャレンジ！！

    $one = fgets(STDIN);
    $two = fgets(STDIN);
    $three = fgets(STDIN);
    
    echo "Gold ".$one;
    echo "Silver ".$two;
    echo "Bronze ".$three;
?>
```